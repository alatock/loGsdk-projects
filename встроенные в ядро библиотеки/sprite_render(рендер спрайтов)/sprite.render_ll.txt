label. #main /<- изменяемые параметры -> r1 = начальный адрес спрайта, содержимое спрайта 
ramw. "c" &10 [1, 1, 2, 2, 3, 3, 58] / удалить при компиляции 
section. parent
assig. r1 '10' / начальный адрес / удалить при компиляции
transf. r1 r3 значение тек.
assig. r4 '1' /curr x coof
assig. r5 '1' /curr y coof
assig. r6 '0' содержимое
assig. r7 '0' x out
assig. r8 '0' y out
    label. #LOOP
    xcord. block
        raml. "m" r6 r3
        if. r6 $ '58' #end
        math. r6 + r4 r7
        out. "r" r7 P1
    ycord. block
        math. "i" r3 '1'
        raml. "m" r6 r3
        if. r6 $ '58' #end
        math. r6 + r5 r8
        out. "r" r8 P2
        math. "i" r3 '1'
    outpt. block
        out. "n" '4' P3
        go. #LOOP
label. #end
    out. "n" '5' P3
halt.